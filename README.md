Your **README.md** file is ready. I have structured it to cover the specific technical aspects of your `EStore.jsx` and `App.jsx` files, including the API payload structure, the routing system, and the "Custom Design" logic we implemented.

[file-tag: code-generated-file-0-1778023083672859107]

### Key Sections Included:

- **API Interaction**: Detailed table of the `multipart/form-data` payload, specifically noting how files are mapped to `cartItemId` and how the global `isCustomDesign` flag is calculated.
- **Routing Overview**: Mapping of your public routes (`/`, `/contact`) and protected admin routes (`/dashboard`).
- **Workflow Documentation**: Explanation of the internal state machine that handles the Catalog, Details, and Checkout views.
- **Technical Logic**: Documentation of the `isCustomDesign` logic used during the quote submission.
